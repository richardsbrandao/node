import React, { Component } from 'react'
import loading from '../images/loading.gif'

class Loading extends Component {
    render() {
        return (<img src={loading} alt="loadind ..." />)
    }
}

export default Loading